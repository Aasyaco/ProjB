{
  "name": "projb-server",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "python3 -c \"import distutils\" 2>/dev/null || python3 -m ensurepip --upgrade && python3 -m pip install --upgrade setuptools",
    "install:rust": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y",
    "build:native": "npm run build:cpp && npm run build:rust",
    "build:cpp": "export HOME=/vercel && cd native/cpp-addon && npm install && npm run rebuild",
    "build:rust": "cd native/rust-validator && ~/.cargo/bin/cargo build --release",
    "build": "npm run install:rust && npm run build:native && npm run build:ts",
    "build:ts": "tsc -p tsconfig.json",
    "rebuild": "node-gyp rebuild"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "dependencies": {
    "helmet": "^7.0.0",
    "axios": "^1.6.8",
    "rate-limiter-flexible": "^2.4.4",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.1.3",
    "node-gyp": "^9.3.1",
    "@types/express": "^4.17.17"
  }
}
